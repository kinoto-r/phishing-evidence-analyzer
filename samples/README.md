# Samples

Do not place real phishing emails in this directory.

Only synthetic or fully sanitized test fixtures may be committed to the public repository.

Real `.eml`, `.msg`, and `.mbox` files are excluded by `.gitignore`.